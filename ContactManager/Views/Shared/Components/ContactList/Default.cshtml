@model IEnumerable<ContactManager.Domain.Entities.Contact>

<table id="contactsTable" class="table">
    <thead>
    <tr>
        <th>Name</th>
        <th>Date of Birth</th>
        <th>Married</th>
        <th>Phone</th>
        <th>Salary</th>
        <th>Actions</th>
    </tr>
    </thead>
    <tbody>
    @foreach (var contact in Model)
    {
        <tr data-id="@contact.Id">
            <td class="editable" data-field="Name">@contact.Name</td>
            <td class="editable" data-field="DateOfBirth">
                @(contact.DateOfBirth.HasValue ? contact.DateOfBirth.Value.ToString("yyyy-MM-dd") : "")
            </td>
            <td class="editable" data-field="Married">
                <input type="checkbox" @(contact.Married ? "checked" : "") disabled />
            </td>
            <td class="editable" data-field="Phone">@contact.Phone</td>
            <td class="editable" data-field="Salary">@contact.Salary</td>
            <td>
                <button class="edit-btn">Edit</button>
                <button class="delete-btn" style="background-color: red; color: white;">Delete</button>
            </td>
        </tr>
    }
    </tbody>
</table>